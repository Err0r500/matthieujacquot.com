// import { fromJS } from 'immutable';
// import { makeSelectInstrumentalDomain } from '../selectors';

// const selector = makeSelectInstrumentalDomain();

describe('makeSelectInstrumentalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
