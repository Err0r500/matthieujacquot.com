// import { fromJS } from 'immutable';
// import { makeSelectLandingDomain } from '../selectors';

// const selector = makeSelectLandingDomain();

describe('makeSelectLandingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
